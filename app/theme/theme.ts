import { createTheme } from "@mui/material";





export const theme = createTheme({
    palette: {

        primary: {
            main: '#9c27b0',
        },
        secondary: {
            main: '#1976d2',
        },
    },

    typography: {
        fontFamily: 'var(--font-geist-roboto), system-ui, sans-serif',
    },

});